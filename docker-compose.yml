version: '3'
services:
  python:
    build: .
    container_name: machine_translation
    working_dir: /home/user/python
    volumes:
      - .:/home/user/python
    ports:
      - 8888:8888
    tty: true
